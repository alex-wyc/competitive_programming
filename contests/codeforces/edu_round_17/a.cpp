#include <bits/stdc++.h>

#define ull unsigned long long

using namespace std;

ull kth_divisor(ull n, ull k) {
    return k;
}

int main(int argc, char *argv[]) {
    ull n, k;

    cin >> n >> k;

    cout << kth_divisor(n, k) << '\n';

    return 0;
}
